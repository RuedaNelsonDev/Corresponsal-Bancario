<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MostrarCorresponsal"
    android:background="@color/black"
    android:orientation="vertical">

    <Toolbar
        android:id="@+id/toolCorresCreados"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/greenOscuro"
        android:title="Corresponsales Creados"
        android:titleTextColor="@color/white"
        android:visibility="gone"/>
    <Toolbar
        android:id="@+id/toolClientesCreados"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/greenOscuro"
        android:title="Clientes Creados"
        android:titleTextColor="@color/white"
        android:visibility="gone"/>

    <include
        android:id="@+id/datosLayout"
        layout="@layout/fila_datos" />
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvMostrarCreados"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </ScrollView>





</LinearLayout>